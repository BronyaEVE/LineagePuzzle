node_modules/
dist/
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
coverage.xml
run/
logs/
.playwright-mcp/
*.egg-info/
.env
backend/data/
.claude/

# 打包产物（pack_portable.bat / pack_desktop.bat 生成，体积大），不进版本库
# 注：frontend/dist 不进版本库（被 frontend/.gitignore 的 dist 规则忽略），
# 部署时靠本地 build 或 GitHub Release 下载便携包。
dist-portable/
dist-offline/
# PyInstaller 构建中间产物（pack_desktop.bat 生成）
build/
# 便携包/桌面版 zip 产物（含版本号命名，如 LineagePuzzle-v2.1.0-portable.zip）
LineagePuzzle-*-portable.zip
*-portable.zip
