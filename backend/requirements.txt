# 生产运行依赖（精确锁定版本，保证离线分发包行为一致）
# 离线打包：pack_portable.bat 用 --index-url 清华源装这些版本

# ---- 引擎（血缘分析核心，单一真相源在 BronyaEVE/lineage_puzzle 仓）----
# v0.1.0 起引擎管线（预处理/拆分/AST 提取/列级映射/归一化）从该包引入，
# 本仓不再内嵌副本（消除双仓分叉）。升版流程：引擎仓修 bug → 打 tag → 改此行。
lineage_puzzle @ git+https://github.com/BronyaEVE/lineage_puzzle.git@v0.1.0

# ---- 应用核心 / App core ----
fastapi==0.135.1
uvicorn[standard]==0.41.0
# sqlglot[c] 用 mypyc 预编译成 C 扩展（.pyd），解析速度比纯 Python 快 3-5 倍。
# 实测：复杂 ETL 语句 parse_one 从 1.16ms → 0.25ms（4.6x）。
# [c] 依赖 sqlglotc 包提供预编译 wheel（cp313-win_amd64），打包时走清华源能拿到。
sqlglot[c]==30.10.0
SQLAlchemy==2.0.49
psycopg2-binary==2.9.10
pydantic==2.12.5
pydantic-settings==2.14.0
filelock==3.29.4
networkx==3.6.1
# 桌面版（PyWebView 窗口模式）依赖：desktop.py 用它开原生窗口加载本地页面。
# 仅桌面版需要，便携包（浏览器访问）模式不需要。
pywebview==6.2.1
